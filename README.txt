Just a little demo library to be able to draw a map and draw
a couple things on top of the map.

To get started:
virtualenv env
source env/bin/activate
pip install -r requirements.txt
./map.py

(go to localhost:5000 in a browser)

